# docs

Project documentation for Memoris.

Planned contents:
- **Architecture** — clear diagram of what Memoris owns vs. what Shelby provides
- **API reference** — backend endpoints (`/memory`, `/agent/{id}`, `/verify`, etc.)
- **SDK reference** — Python and TypeScript client usage
- **Whitepaper** — the case for verifiable agent memory, and why it's built on Shelby rather than as independent infrastructure
- **Deployment guide**

**Status:** not yet written — will follow once Phase 1 architecture is reviewed and approved.
